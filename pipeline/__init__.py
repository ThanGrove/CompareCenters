"""cmpCenters pipeline: crawl peer/CSC sites, extract structured records with
Claude, and produce comparative assessments. Writes into the same database the
Next.js dashboard reads (see docs/adr/0001)."""
