# Python pipeline dependencies (the dashboard's deps are in package.json).
# Install into a venv:  pip install -r requirements.txt
anthropic>=0.71
playwright>=1.56
prisma>=0.15          # prisma-client-py — generated from prisma/schema.prisma
